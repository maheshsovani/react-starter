import React from "react";
import styles from "./Header.module.scss";

function Header() {
  return (
    <header className={styles.header}>
      <span className="logo">Tech-Tenaliram</span>
    </header>
  );
}
export default Header;
